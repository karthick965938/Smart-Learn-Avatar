# Backend API URL — change to your production URL when deploying
VITE_API_BASE_URL=http://127.0.0.1:8000

# Optional: if set, IoT Setup pre-fills and locks these (not editable in the popup)
# VITE_NETWORK_ACCESS_ID=your_wifi_ssid
# VITE_ACCESS_CREDENTIALS=your_wifi_password
# VITE_OPENAI_API_KEY=sk-your-openai-key
